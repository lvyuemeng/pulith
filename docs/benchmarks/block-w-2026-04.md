# Block W Guardrail Notes (2026-04)

Scope:

- cross-codec parity guardrails for lock/state/store snapshot semantics
- repeated ownership/planning path scale expectations in `pulith-state`

Guardrail baselines (current adapter set: pretty-json + compact-json):

- decode parity: pretty-json and compact-json payloads must decode to equal semantic models
- schema mismatch handling: unsupported schema version must surface typed boundary errors (no silent fallback)

Scale envelopes (informational, not hard fail thresholds yet):

- `state_ownership_report` should scale sub-quadratically over 1k -> 10k entries
- `state_repair_plan` should remain monotonic with no abrupt nonlinear jump at 1k/5k entry checkpoints

Alert conditions for follow-up profiling:

- >2x slowdown at 10k entries vs prior median baseline under comparable environment
- loss of parity between pretty-json and compact-json decode roundtrips
- any newly introduced silent fallback path across codec/schema boundaries

Usage:

```bash
cargo bench -p pulith-state --bench ownership_report
cargo bench -p pulith-state --bench repair_plan
```

Latest indexed-vs-direct ownership sample:

- `state_ownership_report_direct/entries/1000`: ~1.89 ms
- `state_ownership_report_direct/entries/5000`: ~15.04 ms
- `state_ownership_report_direct/entries/10000`: ~36.41 ms
- `state_ownership_report_indexed/entries/1000`: ~0.50 ms
- `state_ownership_report_indexed/entries/5000`: ~2.40 ms

Interpretation update:

- reusable analysis index path significantly reduces repeated ownership-report cost versus direct recomputation
- high-entry direct path still scales acceptably for one-shot calls but indexed path is the preferred mechanism for repeated planning/inspection loops

Notes:

- values are environment-sensitive; compare against recent local baseline files rather than single-run absolute numbers
- when guardrail alerts trigger, attach profiler or targeted benchmark slices before algorithm changes
