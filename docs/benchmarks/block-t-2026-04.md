# Block T Benchmark Notes (2026-04)

Scope:

- `pulith-lock` diff scale behavior
- `pulith-state` repair-plan scale behavior

Environment:

- OS: Windows (win32)
- Workspace: `E:\Proj\dev\pulith`
- Runtime: local dev machine (shared background load possible)

Commands:

```bash
cargo bench -p pulith-lock --bench lock_diff
cargo bench -p pulith-state --bench repair_plan
```

Observed medians (single run):

- `lock_diff/entries/100`: ~68.5 us
- `lock_diff/entries/1000`: ~727 us
- `lock_diff/entries/5000`: ~4.22 ms

- `state_repair_plan/entries/100`: ~557 us
- `state_repair_plan/entries/1000`: ~1.47 ms
- `state_repair_plan/entries/5000`: ~6.78 ms

Interpretation:

- both paths scale monotonically across tested sizes
- lock diff remains sub-5ms at 5k entries in this environment
- repair-plan path shows expected growth with snapshot size, without abrupt nonlinear spike in this range

Notes:

- criterion reported outliers and one sample-time warning for `entries/1000`; treat as baseline evidence, not tuning threshold source
- follow-up tuning should use repeated runs and spread summaries before threshold policy changes
