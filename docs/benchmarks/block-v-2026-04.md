# Block V Benchmark Notes (2026-04)

Scope:

- `pulith-state` ownership report scaling after grouped-index rewrite
- `pulith-state` repair-plan scaling sanity check after state/store/resource quality pass

Environment:

- OS: Windows (win32)
- Workspace: `E:\Proj\dev\pulith`
- Runtime: local dev machine, shared background load possible

Commands:

```bash
cargo bench -p pulith-state --bench ownership_report
cargo bench -p pulith-state --bench repair_plan
```

Observed medians (latest run):

- `state_ownership_report/entries/1000`: ~5.45 ms
- `state_ownership_report/entries/5000`: ~31.11 ms
- `state_ownership_report/entries/10000`: ~44.82 ms

- `state_repair_plan/entries/100`: ~465 us
- `state_repair_plan/entries/1000`: ~1.33 ms
- `state_repair_plan/entries/5000`: ~8.64 ms

Interpretation:

- ownership report behavior is no longer quadratic-style explosion in tested range; 10x entries is ~8.2x runtime from 1k to 10k (near-linear trend with overhead)
- repair-plan path remains monotonic; small-size improvement observed, with high-size noise/regression requiring additional repeated runs before threshold decisions

Follow-up note:

- keep `ownership_report` and `repair_plan` benches in routine checks for subsequent algorithm changes
- if 10k-scale ownership variance persists, evaluate optional cached activation-target index across repeated ownership/inspection calls
